# Quickscope, an automation of a few tools
 - koboscan (nmap and rustscan) -- koboscan finds less than sn1per but sn1per finds more and can directly import into msfdb...
 - sn1per
  + into metasploit db
  + possibly run modules based on findings
